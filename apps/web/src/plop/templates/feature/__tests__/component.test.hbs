import { describe, expect, it } from 'vitest';
import { {{pascalCase name}} } from '../components/{{pascalCase name}}';

describe('{{pascalCase name}}', () => {
  it('should be defined', () => {
    expect({{pascalCase name}}).toBeDefined();
  });

  it('should have correct display name', () => {
    expect({{pascalCase name}}.displayName ?? {{pascalCase name}}.name).toBe('{{pascalCase name}}');
  });

  it('should be a valid function component', () => {
    expect(typeof {{pascalCase name}}).toBe('function');
  });

  it('should accept valid props', () => {
    const props = {
      id: 'test-id',
      className: 'test-class',
    };
    
    // Type check should pass
    const validProps: Parameters<typeof {{pascalCase name}}>[0] = props;
    expect(validProps).toEqual(props);
  });

  // Add more test cases here
});